import styled from "styled-components";

const Button = styled.button`
  font-family: inherit;
  font-size: 0.9rem;
  line-height: 0.9rem;
  padding: 0.5rem;
  display: block;
  cursor: pointer;
`;

export default Button;
